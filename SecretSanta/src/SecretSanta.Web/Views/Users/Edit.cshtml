@using Microsoft.Extensions.Configuration
@inject IConfiguration Configuration

<section x-data="SecretSanta.createOrUpdateUser()" x-init="loadData()">
    <script>
        var webHost = "@Configuration["WebHost"].TrimEnd('/')";
    </script>

    @await Html.PartialAsync("_CreateOrEdit")
    <button class="border-2 bg-baby-blue-lighter  hover:bg-baby-blue-dark py-2 px-4 rounded-xl mr-3" @@click="update()">Update</button>
    <a class="inline-block border-2 bg-white border-baby-blue-lighter hover:border-baby-blue-dark py-2 px-4 rounded-xl mt-6" asp-controller="Users" asp-action="Index">Cancel</a>
</section>